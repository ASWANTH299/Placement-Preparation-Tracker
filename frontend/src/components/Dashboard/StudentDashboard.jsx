import PageShell from '../Common/PageShell'

export default function StudentDashboard() {
  return (
    <PageShell title="Student Dashboard" subtitle="Central hub for preparation progress.">
      <p className="text-sm text-slate-600">Dashboard cards and widgets are ready for API-backed data wiring.</p>
    </PageShell>
  )
}
