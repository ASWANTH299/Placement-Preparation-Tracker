After creating the file, give Copilot this short prompt:


Analyze debugging_error.md.

Verify issues against the repository.

Fix all problems while respecting the architecture decisions.

Implement missing features, correct backend APIs, and redesign UI using Tailwind.

After implementation, recheck debugging_error.md and confirm all issues are resolved.




After Copilot finishes:

Recheck debugging_error.md against the codebase.

Confirm every issue is implemented correctly.

Fix anything still missing.