I want you to act as a senior full-stack engineer and refactor my entire project to make it production-ready, consistent across environments, and easy for multiple developers to run.

Follow ALL instructions carefully and do not skip any step.

---

## 🎯 Goal

Ensure the project:

* Runs seamlessly on any machine after cloning
* Has a clean and consistent environment configuration
* Uses a proper admin system (database-driven, not dependent on .env at runtime)
* Has no unused or legacy code
* Follows best practices for team development

---

## 🔧 Phase 1: Environment Standardization

1. Create or update .env.example files for BOTH frontend and backend.
2. Include all required environment variables with clear naming.
3. Ensure .env is not required in repo (only .env.example).
4. Add environment validation in backend:

   * Throw clear errors if required variables are missing.
5. Ensure consistent variable naming across frontend and backend.

---

## 🔐 Phase 2: Admin System Refactor

1. Ensure admin is stored in the database, NOT controlled by .env at runtime.
2. Create a script:

   * scripts/createAdmin.js
   * Reads ADMIN_NAME, ADMIN_EMAIL, ADMIN_PASSWORD from .env
   * Hashes password and creates admin if not exists
3. Add npm script:

   * "seed:admin": "node scripts/createAdmin.js"
4. Remove any logic like:

   * if (email === process.env.ADMIN_EMAIL)
5. Ensure all admin access is controlled via:

   * user.role === "admin"
6. Do NOT auto-create admin on server startup.

---

## 🧹 Phase 3: Code Cleanup

1. Identify and remove:

   * Unused files
   * Unused functions
   * Dead API routes
   * Unused imports
2. Remove legacy fields like phone:

   * From MongoDB schema
   * From database using $unset
   * From frontend forms and API usage
3. Before removing anything, explain what is being removed and why.

---

## 🧱 Phase 4: Consistency & Stability

1. Ensure project works with:

   * npm ci (not npm install)
2. Add .nvmrc with Node version
3. Ensure package-lock.json is consistent
4. Fix any issues causing “works on one machine but not another”

---

## 🗂 Phase 5: Git & Repository Hygiene

1. Review and update .gitignore files for BOTH frontend and backend.
2. Ensure the following are ignored:

   * .env files
   * node_modules/
   * build/dist folders
   * logs (*.log)
   * OS-specific files (.DS_Store, Thumbs.db)
3. Ensure .env.example IS tracked in Git.
4. Remove any accidentally committed sensitive files (like .env) from the repository history if present.
5. Keep the repository clean, minimal, and secure.

---

## 🚀 Phase 6: Developer Experience

1. Add clear setup instructions:

   * Clone
   * Install
   * Setup .env
   * Run project
2. Add helpful error messages
3. Ensure no silent failures

---

## 🧪 Phase 7: Validation

1. Simulate fresh setup:

   * New machine scenario
2. Ensure:

   * No missing env errors
   * Admin can be created using seed script
   * App runs without crashes

---

## 📦 Output Requirements

* Show all updated files
* Clearly explain major changes
* Highlight any breaking changes
* Keep code clean, modular, and production-ready

---

## ⚠️ Important Rules

* Do NOT assume anything — verify from code
* Do NOT remove code without explanation
* Do NOT rely on .env for runtime admin logic
* Prefer clarity over cleverness

---

Now analyze my project and start implementing these improvements step by step.